# Marathon training assistant.
import math

# Prompt user for an integer.
inputNumber = int(input('Please provide an integer: '))
print(str(inputNumber), 'in binary representation is:',bin(inputNumber))
